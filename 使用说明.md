# UCSD 排课助手

## 这是什么

一个在你电脑上本地运行的小程序：

- 用 UCSD Class Planner 官方接口拉取真实课程分班（学期、时间、教室、老师、空位）
- **逐门向导**：一门门选，先定正课（lecture）再定讨论/实验，选的时候实时在日历上看效果；冲突会提示并列出可行替代
- **全部方案模式**：一次性生成所有不冲突的组合，可对比、收藏、导出
- 每门课的卡片里显示完整分班表，老师名下方直接显示 **Rate My Professor** 评分（分数、人数、愿再上比例、难度）
- 用 UCSD 官方 Wayfinding 路线服务估算两栋楼之间的步行时间（官方路线异常离谱时自动退回直线估算）
- **DeepSeek 问答**：填上自己的 API Key 后可以直接问选课建议，程序会自动带上当前所有课程、每个分班的时间/教室/老师/RMP/空位和期末信息

## 怎么运行

1. 确保电脑装了 Python 3.8 以上（没有的话去 python.org 下载，安装时勾选 “Add Python to PATH”）
2. **双击 `start.bat`**，或者打开命令行运行：

   ```bash
   python scheduler.py
   ```

3. 浏览器会自动打开 `http://127.0.0.1:8778/`，开始选课
4. 用完直接关掉命令行窗口，或在窗口里按 `Ctrl+C`

## 使用步骤

1. **添加课程**：输入课程代码，例如 `MATH 100A`、`CSE 29`、`JAPN-020A`（带不带空格、带不带连字符都行），从下拉提示里选中或直接回车
2. **逐门向导（默认）**：左边会按“可行班最少的课优先”推荐顺序，一门门点选正课班和讨论/实验班；每一步右侧日历都会实时更新，确认后再进入下一门；想改任何一门课，点已决定课程旁边的“改”
3. **全部方案模式**：在左边“高级”里切到“全部方案模式”，可以锁定老师、只看空位，然后点“生成全部可行课表”
4. **看课表**：周历下方有课间步行提示和期末考试（按日期）日历；详情里有每天步行明细和非期末的一次性考试
5. **DeepSeek 问答**：填入你自己的 DeepSeek API Key（[platform.deepseek.com](https://platform.deepseek.com) 申请），在输入框写偏好或问题，点“发送问题”；回答会流式显示并自动保存到本地对话记录
6. **导出**：右上角可以导出 PNG / SVG / PDF
7. **收藏**：“保存当前界面”会记住你的课程、向导进度和当前课表，下次想继续时点“加载”就能恢复

## 说明与注意

- 数据来自 UCSD Class Planner 官方接口；学期下拉里目前只有 FA26，后续学期开放后会自动出现
- RMP 评分由程序自动抓取并缓存在 `rmp_cache.json`；抓不到时只显示老师名字
- 步行时间走 UCSD 官方路线服务，少数楼宇匹配不到或路线明显绕路时退回直线估算
- DeepSeek API Key 只存在你本机浏览器（localStorage），不会写进文件，也不会发给本工具以外的任何地方
- 你添加的课程、向导进度、收藏和对话记录会自动保存到同目录的 `saved_data.json`（删除它等于清空状态）
- `buildings_cache.json`、`routes_cache.json`、`rmp_cache.json` 都是缓存，删了会自动重建

## 第一次使用

- 打开程序后，你添加的课程、向导进度、收藏和对话记录会自动保存到同目录的 `saved_data.json`，只存在你自己的电脑上；删除这个文件等于清空状态
- **第一次运行会稍慢**：程序需要拉取楼宇坐标、步行路线和老师评分并建立缓存（通常几十秒），之后就会快很多；这些缓存文件会自动生成在程序目录里，删掉也会自动重建
- 想用 AI 问答的话，需要填**你自己的** DeepSeek API Key（[platform.deepseek.com](https://platform.deepseek.com) 申请）；不填也能正常排课，只是不能用 AI 问答
- 如果提示端口被占用：`python scheduler.py --port 9000` 换端口

## 常见问题

- **没有找到任何可行组合**：试试取消锁定老师、取消“只看空位”，或者少加一门课
- **生成太慢**：取消“估算步行时间”会快很多；组合特别多时耐心等进度条
- **浏览器没自动打开**：手动访问 `http://127.0.0.1:8778/`
- **提示 SSL 证书错误 / 加载学期失败**：程序遇到证书校验失败时会自动改用不校验证书的方式重试一次（仅用于 UCSD 公开数据）；如果仍然失败，多半是网络或代理问题，换个网络或关掉代理再试
